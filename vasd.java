import WeightImpact(sort,0)
PublicClassFrameScheme(0);
		Add value FrameClass; 
	{	 
			chart to
	AddValue1(0)ASCII to Document.Classs
	Weight.SuspendedAlphabetSpiral(0) 
	NULL 
			AddFrameSchemeLock.Reader;

	to
	PublicAddedValue1(0)ASCII
}
